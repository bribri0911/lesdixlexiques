using UnityEngine;

public class GeneratMack : MonoBehaviour
{
    [SerializeField]
    private GameObject[] GetAllMask;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
